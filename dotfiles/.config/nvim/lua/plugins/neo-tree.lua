return {
    "nvim-neo-tree/neo-tree.nvim",
    opts = {
      window = {
        position = "right",
        -- Optional: Set an obvious width to see if *any* options apply
        -- width = 10,
      },
    },
  }